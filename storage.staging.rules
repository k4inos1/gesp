rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // En staging, permitir más acceso para facilitar pruebas
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
