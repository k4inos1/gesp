# Frontend configuration for Render
services:
  - type: web
    name: gesapp-frontend
    runtime: docker
    buildCommand: |
      docker build -t gesapp-angular .
      docker run -d -p 10000:80 gesapp-angular
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
