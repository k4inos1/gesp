# Frontend configuration for Render
services:
  - type: web
    name: gesapp-frontend
    runtime: node
    buildCommand: |
      chmod +x render-build.sh
      ./render-build.sh
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
