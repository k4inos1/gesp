# Frontend configuration for Render
services:
  - type: web
    name: gesapp-frontend
    runtime: node
    buildCommand: |
      npm install
      node render-build.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
