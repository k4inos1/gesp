# Frontend configuration for Render
services:
  - type: web
    name: gesapp-frontend
    runtime: node
    buildCommand: npm run build
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
