specVersion: "v1"
serviceId: "gesapp-angular"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gesapp-angular-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
