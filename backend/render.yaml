# Backend configuration for Render
services:
  - type: web
    name: gesapp-backend
    runtime: node
    buildCommand: |
      npm install
      npm run build:prod
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: "mongodb+srv://zerveck:n74SNV0Gm5i226yc@clustserverkainos.uhwrs.mongodb.net/gesapp?retryWrites=true&w=majority&appName=gesapp-backend"
      - key: JWT_SECRET
        value: your_jwt_secret_key_here
      - key: CORS_ORIGIN
        value: https://gesapp-frontend.onrender.com,http://localhost:4200
    plan: free
